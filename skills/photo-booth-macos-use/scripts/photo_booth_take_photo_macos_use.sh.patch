--- ~/.openclaw/skills/photo-booth-macos-use/scripts/photo_booth_take_photo_macos_use.sh
+++ ~/.openclaw/skills/photo-booth-macos-use/scripts/photo_booth_take_photo_macos_use.sh
@@ -91,7 +91,15 @@
 cat >"$runner_script" <<EOF
 #!/bin/bash
 set -euo pipefail
-bash "$direct_script" --before-shot-delay "$before_shot_delay" --save-timeout "$save_timeout" $( [[ "$reveal_result" == "1" ]] && printf '%s' '--reveal' ) >"$log_file" 2>&1
+output=\$(bash "$direct_script" --before-shot-delay "$before_shot_delay" --save-timeout "$save_timeout" $( [[ "$reveal_result" == "1" ]] && printf '%s' '--reveal' ) 2>&1)
+exit_code=\$?
+printf '%s\n' "\$output" >"$log_file"
+if [[ \$exit_code == 0 ]]; then
+  photo_path="\$(printf '%s\n' "\$output" | tail -n 1)"
+  dest="/tmp/\$(basename "\$photo_path")"
+  cp "\$photo_path" "\$dest" && chmod 644 "\$dest"
+  echo "\$dest" >> "$log_file"
+fi
 echo \$? >"$exit_file"
 EOF
 chmod +x "$runner_script"
