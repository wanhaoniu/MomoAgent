# Deprecated Compatibility Entry

`quick_control_api` has been renamed to `momo_robot_service`.

Use:

```bash
python /Users/moce/Documents/Project/MomoAgent/Software/Master/momo_robot_service/main.py --host 0.0.0.0 --port 8010
```

This directory remains only as a thin compatibility shim for older commands.
